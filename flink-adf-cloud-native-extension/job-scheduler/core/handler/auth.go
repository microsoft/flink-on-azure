package handler

import (
	// "job-scheduler/core/result"
	// "net/http"

	"github.com/gin-gonic/gin"
)

func Auth(c *gin.Context) {
	// _, _, ok := c.Request.BasicAuth()
	// if !ok {
	// 	panic(result.Fail(http.StatusUnauthorized,"auth error",nil))
	// }
}
